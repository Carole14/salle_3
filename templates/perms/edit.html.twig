{% extends 'base.html.twig' %}

{% block title %}Edit Perms{% endblock %}

{% block body %}
    <h1>Edit Perms</h1>

    {{ include('perms/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_perms_index') }}">back to list</a>

    {{ include('perms/_delete_form.html.twig') }}
{% endblock %}
