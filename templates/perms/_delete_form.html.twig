<form method="post" action="{{ path('app_perms_delete', {'id': perm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ perm.id) }}">
    <button class="btn">Delete</button>
</form>
